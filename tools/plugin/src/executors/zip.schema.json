{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "version": 2,
  "title": "Zip executor",
  "description": "Create a zip package of a built plugin project",
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "The output path for the zip file",
      "default": "dist/plugins"
    },
    "sourceRoot": {
      "type": "string",
      "description": "The source root of the plugin project (unused, kept for compatibility)"
    }
  }
}